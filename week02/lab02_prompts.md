# Lab 02 CLI comparison journal

Do not include passwords, tokens, API keys, or complete authentication output.

## Tool check

### GitHub Copilot CLI

I have installed and authenticated GitHub Copilot CLI and it is verison 1.0.83

### Antigravity CLI

I have installed Antigravity CLI and authenticated the tool and it's version is Gemini 3.8 Flash

## Shared task

### Shared prompt

Paste the exact prompt you submitted to both CLI tools.

```text
Could you write a few python functions for me? I need one to perform a make_greeting where it it says "Hello, name!" Name being supplied and then returns the format above with that supplied name. Another one is is_even where it checks if a number is even or odd. If even, it should return true and false otherwise. Last one I need is count_vowels where it can count vowels like a, e, i, o, and u but do not cound y. You can just type it out here, not need to make a file. If you need to ask questions to clarify, please do.
```

### Copilot CLI observations

(I did agy first). Copilot provided the exact same code for make greeting and is_even functions. However, it took the approach of coverting text to lowercase first and then counting the vowels to account for any uppercase. I thought it had forgot to account for uppercase, but corrected me and explained that it would covert to lowercase and not miss any uppercase vowels.

### Antigravity CLI observations

After providing the prompt, it provided python code that would work, but since I was unfamiliar with python, I did ask it a few questions, like for "return number % 2 == 0," explained to me that it checks if the number is divisible by 2 without a remainder and returns the boolean. It also added that this method is btter than a longer if-else statement. I also asked about it's approach for the vowels and it confirmed my understanding of the code. It did also provide the alternative code to do it as a loop.


### Comparison

It looked like both of the responses by the Copilot and the Antigravity CLIs would have performed the fuctions as needed. The only difference between the two was their approach to count_vowels. Copilot coverted the text to lowercase and then checked, while Antigravity had the set have both upper and lowercase. So I did ask each of them to see which they though was more efficient (albeit it's short code) and they both agreed that Antigravity's approach was more efficient as it avoids the lowercase coversion, but both also noted that the set it created each time and each suggested defining the set outside so the set is not recreated each time the function runs. So of course the two functions for make_greeting and is_even were the same, so no decision there, but I chose Antigravity's approach since they agreed it was slightly more efficient.

## Test-guided implementation

As I have stated before, I asked a few questions to make sure that the functions that were generated by Copilot and AGY CLI would have worked and did ask which of their two approaches to each other, which would have been more efficient. As I asked AGY CLI to created the lab02.py for me, it seemed like it found out about the lab02.md instructions and asked me for permission to run the test/grader. It did tell me that the 9 python functions passed, but the journal tests did not as I have not completed this yet, like the preferred tool combination portion of the journal.

## Preferred tool combination

So, I also used the same prompt for browser chat (Claude Sonnet 5 Medium) and the GitHub Copilot in VS Code, and they generated basically the same exact code that Gemini Antigravity came up with the the count_vowels. Which I find a bit funny as the Copilot CLI did not follow the same approach as the Copilot in VS Code. I think the easiest to do for a simple code generation is via the browser chat with Claude, but I can see where the CLI or Copilot in VS Code can be handy when it needs the context of multiple files and it would be much easier to work with those instead of the browser chat. I also have Claude installed in VS Code and the CLI, so I might use those too unless the assignment asks for it. But like I did before, I might still use AGY CLI or Copilot CLI to ask if it has a different idea or approach, and ask them to check each other for the best approach as one model may have a better solution than the other.